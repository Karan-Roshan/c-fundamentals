#include <stdio.h>

int main()
{
    int n, i;
    printf("Enter no.: ");
    scanf("%d %d", &n, &i);

    while (1)
    {
        if (i >= n)
        {
            break;
        }
    }
    return 0;
}